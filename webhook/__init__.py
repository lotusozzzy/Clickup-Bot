"""ClickUp webhook receiver paketi.

webhook.receiver  - Flask uygulaması (POST /clickup-webhook, GET /stats)
webhook.db        - SQLite şema + bağlantı helper'ı
webhook.logger    - FileHandler tabanlı logger (~/clickup-bot/webhook.log)
"""
